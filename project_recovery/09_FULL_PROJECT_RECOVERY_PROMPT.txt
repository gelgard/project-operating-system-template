FULL PROJECT RECOVERY PROMPT — UNIVERSAL MODE 3.0

state_id: {{UNIQUE_MONOTONIC_STATE_ID}}

1. Apply the project response prefix when configured.
2. Full Restore is read-only and follows the Full Context Restore Contract.
3. Read AGENTS, trigger registry and current-state manifest first.
4. Verify Stage/status projections share the same state_id and task state.
5. Reconcile actual worktree/repository, branch, HEAD, staging and dirty state;
   preserve all user changes.
6. Read active architecture, accepted CR register, plans, traceability, active
   task/closure and applicable design/security/domain overlays.
7. Open historical evidence only through current manifest pointers or to
   resolve a named conflict.
8. Run python3 scripts/validate_governance_contract.py and project-specific
   source/spec validators when drift is possible.
9. Return the complete restore certificate and READY/BLOCKED verdict.
10. Stop unless the same OWNER message requests another authorized action.

Never infer current truth from archive order, mtime, chat recency, code or an
implementer PASS. Never mutate, issue work, run external actions or regenerate
historical exports merely because restore succeeded.
