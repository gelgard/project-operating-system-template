RECOVERY PROMPT — UNIVERSAL MODE 3.0

Fast Restore reads AGENTS, trigger registry, current-state manifest,
Stage/status, implementation plan, traceability and applicable overlays.

Full Restore follows docs/architecture/full-context-restore-contract.md. It is
read-only, manifest-first, reconciles actual version control and opens
historical evidence only through current pointers or conflict investigation.

Run python3 scripts/validate_governance_contract.py. Return the completeness
certificate with state_id, actual repository facts, current/corrective/next
task, Outcome Slice, Requirement delivery/acceptance, CR-trigger coverage,
design/security/external boundaries, evidence, human walkthrough state,
budgets, drift and separate readiness verdicts.

Contradictory current projections, missing accepted-CR binding, stale
placeholders in project mode or manifest/repository drift are BLOCKED.

After restore, stop unless the same OWNER message explicitly requests another
authorized action.
